# Changelog

## Unreleased

- Phase 1 scaffold: event types (`src/events.ts`), `StdoutSink` + `HttpSink` (`src/sinks.ts`), `baton-spec` submodule + wire-conformance test. Not yet published to npm — no MCP capture surface (`withBaton`) yet.
